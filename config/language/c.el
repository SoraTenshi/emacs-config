;; Setting up emacs for the c programming language
;;; Code:

(use-package cc-mode
  :hook (c-mode))

;;; c.el ends here
;;; Commentary:
;; Setting up EMACS for the c programming language
;;; Code:

(use-package cc-mode
  :ensure t
  :straight t
  :hook (cc-mode))

;;; c.el ends here
